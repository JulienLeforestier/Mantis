<nav class="navbar navbar-expand-lg navbar-dark fixed-top bg-dark text-light">
	<a class="navbar-brand" href="{{ path("home") }}"><img class="img" src="{{ url_images }}logo.png" alt="logo" width="40"/></a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>

	<div class="collapse navbar-collapse" id="navbarSupportedContent">
		<ul class="navbar-nav mr-auto ml-auto">
			{% if not is_granted('IS_AUTHENTICATED_FULLY') %}
				<li class="nav-item">
					<a class="nav-link" href="{{ path("app_register") }}">
						<i class="fas fa-door-open"></i>
						Inscription
					</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path("app_login") }}">
						<i class="fas fa-sign-in-alt"></i>
						Connexion
					</a>
				</li>
			{% else %}
				<li class="nav-item">
					<a class="nav-link" href="{{ path("profile") }}">
						<i class="fas fa-user"></i>
						Espace Client
					</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path("app_logout") }}">
						<i class="fas fa-sign-out-alt"></i>
						Déconnexion
					</a>
				</li>
			{% endif %}
			{% if is_granted('ROLE_ADMIN') %}
				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle" href="#" id="menuadmin" data-toggle="dropdown"><i class="fas fa-users-cog"></i>Admin</a>
					<div class="dropdown-menu" aria-labelledby="menudamin">
						<a class="dropdown-item" href="{{ path("user_index") }}">Membres</a>
						<a class="dropdown-item" href="{{ path("product_index") }}">Produits</a>
						<a class="dropdown-item" href="{{ path("command_index") }}">Commandes</a>
						<a class="dropdown-item" href="{{ path("category_index") }}">Catégories</a>
						<a class="dropdown-item" href="{{ path("type_index") }}">Genres</a>
						<a class="dropdown-item" href="{{ path("mark_index") }}">Éditeurs</a>
						<a class="dropdown-item" href="{{ path("producer_index") }}">Producteurs</a>
						<a class="dropdown-item" href="{{ path("promotional_code_index") }}">Codes promotionnels</a>
						<a class="dropdown-item" href="{{ path("notice_index") }}">Avis</a>
					</div>
				</li>
			{% endif %}
		</ul>
		<ul class="navbar-nav mr-2">
			<li class="nav-item">
				<a class="nav-link" href="{{ path("cart") }}">
					<i class="fas fa-shopping-cart"></i>
					Panier
				</a>
			</li>
		</ul>
	</div>
</nav>

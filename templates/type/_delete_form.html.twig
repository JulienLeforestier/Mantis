<form method="post" action="{{ path('type_delete', {'id': type.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce genre?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type.id) }}">
	<button class="btn btn-primary">Supprimer</button>
</form>

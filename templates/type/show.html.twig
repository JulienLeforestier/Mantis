{% extends 'base.html.twig' %}

{% block title %}Genre{% endblock %}

{% block body %}
    <h1>Genre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ type.title }}</td>
            </tr>
        </tbody>
    </table>

	<a href="{{ path('type_index') }}" class="btn btn-primary">Retour</a>

	<a href="{{ path('type_edit', {'id': type.id}) }}" class="btn btn-primary">Éditer</a>
	<br><br>
    {{ include('type/_delete_form.html.twig') }}
    <br>
{% endblock %}

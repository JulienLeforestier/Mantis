<form method="post" action="{{ path('notice_delete', {'id': notice.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet avis?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ notice.id) }}">
	<button class="btn btn-primary">Supprimer</button>
</form>
